# 포트폴리오 평가서 — 게임 회사 채용 담당자 관점

> 작성 기준: 게임 클라이언트 프로그래머 신입/주니어 지원자의 웹 포트폴리오를
> 서류 검토 단계에서 **평균 60초** 안에 훑어보는 상황을 가정했습니다.

---

## 1. 총평

**개선 전 : 통과 가능하지만 아깝다 (B-)**
소재는 충분히 좋습니다. UE5 멀티플레이어, VR, LLM NPC는 신입 포트폴리오에서 흔치 않은 조합이고,
Blender와 영상 편집까지 직접 한다는 점은 실무에서 확실히 쓰입니다.
문제는 **그 좋은 소재가 검증 불가능한 형태로 놓여 있었다**는 것입니다.

| 항목 | 개선 전 | 개선 후 | 코멘트 |
|---|:---:|:---:|---|
| 첫인상 (3초 안에 직무 파악) | ★★☆☆☆ | ★★★★★ | "저는 강원우입니다"만으로는 무슨 직군인지 알 수 없었음 |
| 정보 구조 (원하는 것을 찾기) | ★★☆☆☆ | ★★★★☆ | 프로젝트가 페이지 최하단, 내비게이션 링크는 깨져 있었음 |
| 기술 증빙 (믿을 근거) | ★☆☆☆☆ | ★★★☆☆ | 코드·영상 링크 0건. 수치 0건. **최대 감점 요인** |
| 프로젝트 서술 | ★★☆☆☆ | ★★★★☆ | 기능 나열만 있고 "왜 그렇게 했는지"가 없었음 |
| 디테일 완성도 | ★★☆☆☆ | ★★★★☆ | 복붙 오류·오타·깨진 이미지 다수 |
| 개인정보 관리 | ★☆☆☆☆ | ★★★★★ | 휴대폰 번호와 상세 주소가 공개 노출 |

---

## 2. 면접관이 실제로 보는 순서와, 거기서 걸린 것들

### ① 첫 화면 (3초)
개선 전 첫 화면 문구는 **"만나서 반갑습니다! / 저는 강원우입니다!"** 뿐이었습니다.
채용 담당자는 하루에 수십 개의 포트폴리오를 봅니다. 첫 화면에서
**직무 · 주력 기술 · 대표 성과**가 보이지 않으면 그 시점에 이탈합니다.

→ 히어로 영역에 `Game Client Programmer` 배지, 한 줄 소개, 기술 칩,
그리고 **"대표 프로젝트 보기" CTA**를 배치했습니다.

### ② 프로젝트 (30초)
가장 중요한 섹션인데 **페이지 맨 아래**에 있었습니다.
그리고 상세 내용이 이런 식이었습니다.

> Key Features
> - C++ 기반의 서버-클라이언트 리플리케이션 구현
> - Steam SDK를 활용한 P2P 매칭 및 세션 관리

이건 **기능 목록**이지 **경험 서술**이 아닙니다. 면접관 입장에서는
"이걸 정말 본인이 했는지", "어떤 문제가 있어서 그렇게 했는지"를 알 수 없습니다.

→ 프로젝트 섹션을 About·Skills보다 **앞으로 이동**시키고,
모든 상세 내용을 아래 5단 구조로 다시 썼습니다.

```
문제 상황  →  내가 맡은 일  →  해결 방법(선택의 이유 포함)  →  결과(수치)  →  배운 점
```

특히 **"해결 방법"에 판단 근거를 넣은 것**이 핵심입니다.
예를 들어 Noob 프로젝트는 "리플리케이션 구현" 대신
"눈에 보이는 연출은 Multicast RPC, 지속 값은 Replicated 변수 + OnRep으로 나눠 대역폭을 줄였다"로 바꿨습니다.
면접관은 **기술 이름이 아니라 선택의 이유**로 실력을 판단합니다.

### ③ 검증 (10초)
> **가장 큰 감점 요인이었습니다.**
> GitHub 링크, 플레이 영상, 스크린샷, 수치 — **하나도 없었습니다.**

포트폴리오 사이트가 GitHub Pages에 올라가 있는데 정작
**프로젝트 저장소로 가는 링크가 단 하나도 없다는 것**은 치명적입니다.
채용 담당자는 "보여주기 싫은 건가, 없는 건가"를 의심하게 됩니다.

→ 각 프로젝트에 `links` 필드를 추가했습니다. 아직 URL이 비어 있는 항목은
화면에 **"(준비 중)"** 으로 표시되도록 해 두었으니, **가장 먼저 채워야 할 항목**입니다.

### ④ 스킬 (10초)
개선 전에는 9개 기술이 **평면적으로 나열**되어 있었습니다.
신입 지원자가 UE5·Unity·C++·C#·Python·Blender·Photoshop·Premiere·AI를
모두 같은 비중으로 적으면, 면접관은 **"전부 얕게 했겠구나"**로 읽습니다.

→ `Core(주력) / Working(실무 활용) / Supporting(보조)` 3단계로 나누고,
각 항목에 **그 기술을 실제로 쓴 프로젝트 이름**을 붙였습니다.
솔직한 계층 구분은 오히려 신뢰를 높입니다.

### ⑤ 자기소개 (10초)
"새로움을 즐기는 개발자", "비즈니스 가치를 우선하는 개발자" — 문장 자체는 좋지만
**어느 지원자나 쓸 수 있는 말**이었습니다.

→ 각 강점 뒤에 **실제 프로젝트에서의 근거**를 붙였습니다.
예: "완성되지 않으면 의미가 없다" → "Capstone VR에서 표현 욕심을 줄이고 프레임 예산을 먼저 확보했다".

---

## 3. 발견된 버그 및 실수 (모두 수정 완료)

디테일 문제는 그 자체보다 **"본인 사이트도 검수하지 않는 사람"** 이라는 인상을 준다는 점에서 위험합니다.

| # | 문제 | 영향 |
|---|---|---|
| 1 | 내비게이션의 `#main-projects-section` 앵커가 **존재하지 않음** | 메뉴 클릭 시 아무 반응 없음 |
| 2 | 내비게이션의 `#contact-section` 섹션이 **존재하지 않음** | 연락처 메뉴가 죽은 링크 |
| 3 | `projectData` 이미지 경로 5개 오타 (`p-light.jpg`, `p-web.jpg`, `p-laonzena.jpg`, `p-face.jpg`, `p-niagara.jpg` — 실제 확장자는 `.png` 또는 다른 파일명) | 상세 보기 사이드바 썸네일 전부 깨짐 |
| 4 | `images/resume.jpg` 파일 부재 | "이력서 보기" 클릭 시 **빈 창** |
| 5 | Python 설명이 C++ 설명 복붙 ("메모리 최적화와 디자인 패턴… STL 활용") | Python에 STL은 존재하지 않음 → **기술 이해도 의심** |
| 6 | Photoshop · Premiere 설명이 Generative AI 설명과 **동일한 문장 3회 반복** | 성의 부족으로 읽힘 |
| 7 | `Genrative AI` 오타 (→ Generative) | 영문 표기 신뢰도 하락 |
| 8 | `<html lang="en">` 인데 본문은 한국어 | 스크린리더·SEO 문제 |
| 9 | README가 **템플릿 원본 그대로** ("Profolio는 개발자를 위한 정적 포트폴리오 템플릿입니다") | 저장소 방문 시 **본인 작업물로 보이지 않음 — 가장 아쉬운 부분** |
| 10 | 휴대폰 번호 · 상세 주소(동 단위) 공개 노출 | 스팸·보이스피싱 표적. 채용에는 이메일이면 충분 |
| 11 | 메인/사이드 프로젝트 분류가 내비게이션 라벨과 불일치 | 정보 구조 혼란 |
| 12 | `<meta description>`, OG 태그 부재 | 링크 공유 시 미리보기 없음 |

---

## 4. 이번에 수정한 내용

- **`index.html`**
  - 섹션 순서 재배치 : `Hero → Projects → Skills → About → Contact`
  - 히어로 전면 재작성 (직무 배지 · 태그라인 · 기술 칩 · CTA 3종)
  - `Contact` 섹션 신설, 내비게이션 앵커 전부 정상화
  - 스킬을 3단계 숙련도로 재편, 중복·오류 설명 전부 교체
  - About을 "요약 → 강점 3가지(근거 포함) → 기본 정보"로 재구성
  - 휴대폰 번호·상세 주소 제거, GitHub 링크 추가
  - `lang="ko"`, meta description, OG 태그 추가
  - 이력서 이미지 로드 실패 시 PDF 링크로 대체되는 폴백 처리
- **`js/main.js`**
  - `projectData` 전면 재작성 (5단 구조 + `period` / `team` / `links` 필드 추가)
  - 이미지 경로 오류 5건 수정
  - 상세 뷰 렌더링에 대표 이미지 · 메타 정보 · 링크 버튼 추가
- **`css/style.css`** — 신규 컴포넌트 스타일 추가 (파일 하단), 모바일 히어로 레이아웃 보정
- **배경 영상 폴백** — 영상 로딩 실패 시 화면 절반이 흰색으로 비던 문제를 어두운 배경색으로 처리
- **프로젝트 상세 진입 시 스크롤** — 카드를 눌렀을 때 빈 공간만 보이던 현상을 부드러운 스크롤로 해결
- **`README.md`** — 템플릿 원본을 본인 포트폴리오 소개로 교체
- **`PORTFOLIO_REVIEW.md`** — 본 문서

---

## 5. 남은 숙제 (직접 해야 하는 것)

우선순위 순입니다. **1번과 2번을 하지 않으면 이번 개편의 효과가 절반 이하로 떨어집니다.**

1. **`<em class="ph">○○</em>` 자리를 실제 수치로 교체**
   화면에 분홍 점선으로 표시되어 있습니다. 정확한 값을 모르면 측정하세요.
   Unreal Insights, `stat unit`, Network Profiler로 대부분 뽑을 수 있습니다.
   *수치를 못 채우겠다면 그 문장은 지우는 편이 낫습니다. `○○`가 남아 있는 게 최악입니다.*
2. **GitHub 저장소 · 플레이 영상 링크 채우기** (`js/main.js` 의 `links`)
   저장소가 비공개라면 최소한 **1~2분짜리 플레이 영상**이라도 YouTube에 올려 연결하세요.
   면접관은 코드보다 **"실제로 돌아가는 화면"** 을 먼저 봅니다.
3. **프로젝트별 상세 스크린샷 2~3장 추가**
   현재는 썸네일 1장뿐입니다. 특히 Noob의 네트워크 구조도, Capstone VR의 프로파일링 결과 캡처가 있으면 설득력이 크게 올라갑니다.
4. **`images/resume.jpg` 추가** (이력서 미리보기용)
5. **Google Drive 이력서 링크의 공유 권한 확인**
   "링크가 있는 모든 사용자" 로 되어 있는지 반드시 확인하세요. 권한 오류는 즉시 이탈로 이어집니다.
6. **자기소개 문구 최종 검토**
   제가 쓴 근거 문장(예: Capstone VR의 판단 사례)이 실제 경험과 다르다면 반드시 본인 경험으로 고쳐 주세요.
   **면접에서 그대로 질문받는 부분입니다.**
7. **About 영역 사진 교체 검토**
   현재 슬라이드에 들어간 사진들은 사적인 스냅에 가깝습니다. 개성을 보여 주는 것 자체는 좋지만,
   **작업 중인 모습이나 프로젝트 화면**으로 한 장이라도 바꾸면 "개발자의 포트폴리오"라는 인상이 훨씬 강해집니다.
8. **모바일에서 한 번 열어 보기** — 채용 담당자의 상당수가 휴대폰으로 먼저 확인합니다.
   (이번 개편에서 모바일 히어로 버튼 배치와 스크롤 아이콘 겹침은 수정해 두었습니다.)

---

## 6. 2차 개편 — 레이아웃 정리와 Learning 섹션

첫 개편 이후 레이아웃이 섞여 보이고 글자가 겹친다는 피드백을 받아 다음을 정리했습니다.

**겹침 · 잘림 문제**

- 템플릿의 거대한 워터마크 제목(`.big` / `.big-2`)이 실제 제목과 **겹쳐서 잘려 보이던 문제**를 제거했습니다.
  (`About` 위에 `About Me`, `Portfolio` 위에 `Selected Projects` 가 포개져 있었습니다.)
- Skills 섹션 왼쪽의 목차(`#navi`)가 화면의 1/3을 비운 채 본문을 오른쪽으로 밀던 구조를 없애고 전체 폭을 사용합니다.
- 섹션 사이에 화면 하나 분량의 빈 공백이 생기던 문제를 공통 여백 규칙(`.pf-section`)으로 통일했습니다.
- 스킬 아이콘 이미지 우하단에 흰 조각이 보이던 현상(원본 PNG에 포함된 여백)을 크롭으로 처리했습니다.

**섹션 순서**

읽는 순서를 그대로 따르도록 재배치했습니다.

```
Hero → About(누구인가) → Skills(무엇을 할 수 있나) → Projects(무엇을 만들었나)
     → Learning(어떻게 성장하나) → Background(학력·자격) → Contact
```

**디자인 시스템 정리**

- 커스텀 스타일을 `css/portfolio.css` 한 파일로 분리했습니다. 색·여백·폰트는 파일 맨 위 CSS 변수로 관리합니다.
- 한글 가독성을 위해 Pretendard 폰트를 적용했습니다.
- 섹션 헤더(작은 라벨 + 큰 제목 + 설명)와 카드 컴포넌트를 하나의 규칙으로 통일했습니다.

**Learning 섹션 신설**

읽은 책과 수강한 강의를 탭으로 전환해 볼 수 있게 했습니다. 면접관 관점에서 이 섹션의 핵심은
목록 자체가 아니라 각 항목 하단의 **"적용" 한 줄**입니다.
"게임 프로그래밍 패턴을 읽었다"는 흔하지만, "그래서 Noob의 어빌리티 시스템을 이렇게 모듈화했다"는 흔하지 않습니다.
현재 내용은 예시이므로 반드시 실제 목록으로 교체하세요.

---

## 7. 3차 개편 — Unreal Editor 다크 테마

지원 직군이 게임 클라이언트인 만큼, 사이트 자체가 **작업 환경을 닮게** 톤을 바꿨습니다.
레이아웃과 문구는 그대로 두고 스킨만 교체했으므로, 구조를 다시 검토할 필요는 없습니다.

| 요소 | 어떻게 바꿨나 |
|---|---|
| 전체 배경 | UE5 에디터의 3단 계층 (`#151515` 바탕 → `#1e1e1e` 패널 → `#262626` 카드) |
| 포인트 컬러 | UE5 선택·활성 블루 `#0e86d4` |
| 섹션 라벨 | 패널 탭 형태 (상단 액센트 라인 + 사각 테두리) |
| 기본 정보 / 프로젝트 메타 | Details 패널의 프로퍼티 행 (라벨 열 + 구분선 + 줄무늬 배경) |
| 스킬 분류 헤더 | Details 패널의 카테고리 행 (▾ 마커 + 좌측 액센트 바) |
| 프로젝트 목록 | 콘텐츠 브라우저의 에셋 타일 (얇은 테두리, 선택 시 블루 링) |
| 책 · 강의 | 에디터 탭 바 (활성 탭 상단 액센트) |
| 히어로 | 뷰포트 프레임 + 코너 HUD 라벨 |
| 라벨 · 수치 | 고정폭 글꼴로 통일 |
| 스크롤바 | 다크 에디터 스타일 |

한 가지 주의할 점이 있습니다. **다크 테마는 사진과 이미지의 품질을 그대로 드러냅니다.**
밝은 배경에서는 적당해 보이던 스크린샷도 어두운 패널 위에서는 노이즈와 압축 흔적이 눈에 띕니다.
프로젝트 썸네일을 교체할 계획이라면 이번 기회에 함께 정리하는 편이 좋습니다.

---

## 8. 4차 개편 — 뷰포트 요소와 3D 모델링 모티프

에디터 스킨에서 한 걸음 더 들어가, 실제로 **뷰포트를 조작하는 느낌**과 **메시를 다루는 모티프**를 넣었습니다.

| 요소 | 내용 |
|---|---|
| 와이어프레임 메시 | 히어로 중앙에서 천천히 회전하는 아이코스피어. 라이브러리 없이 정이십면체를 1회 분할해 직접 투영합니다 (42 버텍스 / 120 엣지). 깊이에 따라 선 굵기와 밝기가 달라집니다 |
| 뷰 모드 전환 | 상태 표시줄의 **LIT / UNLIT / WIREFRAME** 버튼이 실제로 화면을 바꿉니다. 와이어프레임에서는 영상이 물러나고 격자와 메시가 전면에 나옵니다 |
| 좌표축 기즈모 | 우하단 X·Y·Z 축 표시기 |
| 블루프린트 노드 그래프 | 프로젝트 정리 방식(문제 → 해결 → 결과)을 실행 핀과 와이어가 있는 노드 3개로 표현 |
| 에셋 선택 마퀴 | 프로젝트 카드에 마우스를 올리면 와이어프레임 격자와 네 모서리 브래킷이 나타납니다 |
| 버텍스 점 | 섹션 경계선에 격자 간격으로 정점 표시 |

### 성능에 대해

게임 클라이언트 지원자의 포트폴리오에서 **프레임이 떨어지는 것은 그 자체로 감점**이라고 판단해,
상태 표시줄에 실측 FPS를 띄워 두고 작업했습니다. 실제로 다음을 잡았습니다.

- 배경 영상에 CSS `filter` (grayscale·contrast) 를 걸자 **60 → 29 FPS**.
  영상 레이어에 필터가 걸리면 합성될 때마다 필터 패스가 돕니다. `opacity` 와 단색 오버레이로 교체.
- 전면 `mask-image` 와 `box-shadow: inset 0 0 0 9999px` 도 원인이었습니다.
  마스크 대신 배경색 래디얼 그라데이션을 겹쳐 같은 페이드를 만들고, 거대 인셋 섀도는 제거.
- `backdrop-filter` 제거, 와이어프레임 모드에서는 영상 디코딩 자체를 중지.
- 메시 렌더링은 화면 밖으로 나가거나 다른 탭으로 가면 멈추고,
  `prefers-reduced-motion` 을 켠 사용자에게는 한 장만 그리고 정지합니다.

**결과 : 세 모드 모두 60 FPS.** 면접에서 물어보기 좋은 소재이기도 합니다.

---

## 9. 5차 개편 — 미디어 경량화와 UI 재구성

### 미디어 : 106MB → 6MB

가장 큰 문제였습니다. 배경 영상 하나가 **94MB** (1920×1080, 105초, 7.5Mbps) 였고,
프로젝트 썸네일도 장당 1~2MB 였습니다. 휴대폰으로 열면 한참 검은 화면만 보이는 상태였습니다.

| | 전 | 후 |
|---|---|---|
| `video.mp4` | 94 MB | **4.2 MB** (720p / 24fps / CRF 36 / 무음) |
| 이미지 전체 | 13 MB | **1.4 MB** (리사이즈 + 재인코딩) |
| **첫 로드 (데스크톱)** | 약 106 MB | **6.1 MB** |
| **첫 로드 (모바일)** | 약 106 MB | **2.0 MB** |

모바일과 데이터 절약 모드에서는 영상을 아예 받지 않고 포스터 이미지(69KB) 한 장만 씁니다.
템플릿 잔재(`demo/20210428.gif` 3.6MB, `scss/`, `single.html`)도 정리했습니다.

> 게임 클라이언트 지원자에게 **에셋 용량 관리는 그 자체로 평가 항목**입니다.
> 압축 전 상태는 정반대 신호를 주고 있었습니다.

### Skills — 로고 카드 유지

한 차례 블루프린트 노드로 바꿔 봤으나 노드 느낌이 과해서 **기존 로고 카드 UI로 되돌렸습니다.**
대신 이미지 최적화의 효과는 그대로 남습니다.

- 스킬 아이콘 22장 합계 **4.5MB → 0.46MB**
- 화면 아래쪽에 있으므로 `loading="lazy"` 로 지연 로드 —
  첫 화면에서는 받지 않고, 스크롤이 닿을 때만 불러옵니다 (모바일 기준 +0.14MB)
- 티어 헤더(`▾ 주력 [CORE]`)도 기존 형태를 유지했습니다

### Learning — 콘텐츠 브라우저로

폴더 트리 · 에셋 그리드 · 상세 패널의 3분할 구조입니다.
상태(완독/읽는 중/예정)는 에셋 타일 하단의 색 띠로, 검색과 항목 수 표시까지 갖췄습니다.

### 그 밖에

- **프로젝트 딥링크** : `#project/noobgame` 형태로 특정 프로젝트를 링크로 공유할 수 있고,
  브라우저 뒤로가기로 닫힙니다
- **키보드 접근성** : 카드를 Tab 으로 이동하고 Enter / Space 로 열 수 있습니다.
  콘텐츠 브라우저는 방향키로 항목을 이동합니다
- **파비콘** : 외부 파일 없이 인라인 SVG 로 와이어프레임 큐브
- Projects 헤더의 블루프린트 노드 그래프는 제거했습니다 (Skills 로 이동)

---

## 10. 6차 개편 — 대표 프로젝트 쇼케이스와 에디터 패널 크롬

**대표 프로젝트 쇼케이스** — 카드 3장 나열에서, 큰 미리보기 하나 + 아래 썸네일 스트립 구조로 바꿨습니다.
썸네일을 고르면 큰 화면이 크로스페이드로 전환되고, 오른쪽 패널에 제목·기술·역할·성과·기간·규모가 함께 표시됩니다.
자동 전환은 넣지 않았습니다 — 성과 문구를 읽는 중에 화면이 바뀌는 게 가장 나쁩니다.

**Side Projects → 아웃라이너** — 실제 언리얼 에디터의 아웃라이너를 참고해
`Item Label | Type` 두 컬럼 목록으로 바꿨습니다. 눈 아이콘, 확장 캐럿, 줄무늬 행, 검색,
하단 `7 items` 카운트까지. 카드 그리드보다 세로 길이가 40% 짧고 훑어보기도 쉽습니다.

**패널 크롬 통일** — 탭(✕ 포함) + 필터 행 + 하단 상태 행을 `.ue-panel` 로 묶어
아웃라이너와 콘텐츠 브라우저가 같은 규칙을 쓰게 했습니다.

**About 기본 정보** — Details 패널의 카테고리 헤더(`▾ BASIC INFO`)를 붙였습니다.

**푸터 → 상태 표시줄** — 좌측 이동 링크 / 우측 `Open to work`, `KINGWONWOO / main` 브랜치 표시.

---

## 11. 인트로 스플래시

첫 방문 시 언리얼 시작 화면 형태의 인트로를 띄웁니다.
와이어프레임 큐브가 그려지고, 진행 바와 함께
`INITIALIZING ENGINE → LOADING LVL_PORTFOLIO → COMPILING SHADERS → BUILDING CONTENT BROWSER → READY`
로 상태가 바뀝니다.

**연출이 방문자를 붙잡지 않도록** 아래 규칙을 걸어 두었습니다.

- 진행률은 **실제 페이지 로드에 연동** — 로드가 끝나기 전까지는 92%에서 멈추고, 끝나면 100%로 채워집니다
- 최소 1.2초 / **최대 3.6초** — 리소스가 아무리 느려도 그 이상 잡아 두지 않습니다
- 아무 곳이나 클릭하거나 Esc / Enter / Space 로 즉시 건너뜁니다 (안내 문구 표시)
- **같은 탭에서 다시 방문하면 표시하지 않습니다** (`sessionStorage`) —
  새로고침할 때마다 나오면 채용 담당자에게는 방해일 뿐입니다
- `prefers-reduced-motion` 사용자에게는 아예 표시하지 않습니다

---

## 12. 마지막으로

기술 스택은 이미 충분합니다. 부족한 건 실력이 아니라 **증거**입니다.
같은 프로젝트라도 이렇게 바뀝니다.

> ❌ "Steam SDK를 활용한 P2P 매칭 및 세션 관리"
> ⭕ "4인 세션에서 30분 연속 플레이 중 데스싱크 0건. 판정을 서버로 일원화해 상태 불일치를 구조적으로 차단"

면접관이 궁금한 건 **무엇을 썼는가**가 아니라 **무엇을 판단했고 그래서 어떻게 되었는가**입니다.
남은 숙제 1·2번만 채워도 이 포트폴리오는 서류 통과 확률이 눈에 띄게 올라갑니다.
